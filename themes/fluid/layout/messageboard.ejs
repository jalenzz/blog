<%
page.layout = "messageboard"
page.title = theme.messageboard.title || __('messageboard.title')
page.subtitle = theme.messageboard.subtitle || __('messageboard.subtitle')
page.banner_img = theme.messageboard.banner_img
page.banner_img_height = theme.messageboard.banner_img_height
page.banner_mask_alpha = theme.page404.banner_mask_alpha
%>

<div class="markdown-body">
    <img src="https://cdn.jsdelivr.net/gh/Royce2019/BlogSource/messageboard/signage.webp">
</div>

<div class="text-center">
    不知不觉&nbsp;<span id="gradient" style="font-size:26px;">...</span>&nbsp;天过去了<br><br>
    🍭 欢迎你的来访<br><br><br>
</div>
<script>
    var now = new Date();
    function createtime(){
        var grt = new Date("11/20/2019");//此处修改你的建站时间或者网站上线时间
        now.setTime(now.getTime()+250);
        days = Math.floor((now - grt ) / 1000 / 60 / 60 / 24);
        document.getElementById("gradient").innerHTML = days;
    }
    setInterval("createtime()",250);
</script>

<!-- Comments -->
<div class="container comments mx-auto" id="comments">
    <% if(theme.post.comments.enable) { %>
    <br><br>
    <% var type = '_partial/comments/' + theme.post.comments.type %>
    <%- partial(type) %>
    <% } %>
</div>